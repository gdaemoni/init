#	Утилита netstat отображает содержимое различных структур 
#	данных, связанных с сетью
#
#	-r Показать таблицы маршрутизации.
#	-n Показать сетевые адреса в виде чисел

netstat -rn | grep default | grep en0 | cut -c-31
